<ul>
<% @people.each do |p| %>
  <li>
    <%=link_to(p.full_name,person_path(p))%>
    <%= link_to("[edit]",edit_person_path(p), method: :get) %>
    <%= link_to("[delete]",person_path(p), method: :delete, data: { confirm: 'Are you sure?' }) %>
  </li>
<%end%>
</ul>
